# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory to change to before starting a build.
  # This is where we will look for package.json/.nvmrc/etc.
  base = "studio/"

  command = "npm i && npm run bundle"

  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build. If a base directory has
  # been specified, include it in the publish directory path.
  publish = "studio/dist/"

# Statebox Studio is a SPA that handles routing internally
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Publish generated source code documentation
[context.docs]

  # This is the top-level project, so all its dependencies (stbx-core,
  # stbx-rest-client, etc.) will be included.
  base = "studio/"

  command = "npm i && npm run docs"

  publish = "studio/generated-docs/"
